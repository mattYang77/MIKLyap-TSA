[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "miklyap-tsa"
version = "0.1.0"
description = "Core parametric Koopman model, analytical Lyapunov, root-finding, SMT, and local saddle eigenfunction components."
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.26,<3",
    "scipy>=1.11,<2",
    "torch>=2.4,<3",
]

[tool.setuptools.packages.find]
where = ["src"]
